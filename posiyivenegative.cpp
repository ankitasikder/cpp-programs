#include<iostream>
#include<conio.h>
using namespace std;
int main()
{
     int a;
     cout<<"Enter the no.-->";
     cin>>a;
     if(a>0)
     cout<<"a is a positive no.";
     else if (a==0)
     cout<<"a is o";
     else 
     cout<<"a is a negative no";
     getch();
     return 0;
}
